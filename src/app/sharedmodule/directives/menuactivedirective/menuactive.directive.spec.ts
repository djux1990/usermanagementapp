import { MenuactiveDirective } from './menuactive.directive';

describe('MenuactiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuactiveDirective();
    expect(directive).toBeTruthy();
  });
});
