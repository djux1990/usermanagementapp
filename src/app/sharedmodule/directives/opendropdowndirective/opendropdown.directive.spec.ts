import { OpendropdownDirective } from './opendropdown.directive';

describe('OpendropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new OpendropdownDirective();
    expect(directive).toBeTruthy();
  });
});
